import { Router } from '@angular/router';
import { DataResolver } from './data.resolver';
import { Observable } from 'rxjs/Observable';
import { Result } from '../models/result.model';
export declare class ModalService {
    private router;
    private resolver;
    results: Result<any>[];
    constructor(router: Router, resolver: DataResolver);
    getValue(): any;
    open<T>(name: string, data?: Observable<T> | Promise<T> | T): Result<T>;
    close(): void;
    ok(value?: any): void;
    cancel(): void;
}
